#include<stdio.h>
#include<math.h>
#include<math.h>
int main()

{
   double t,i,j, x,y;
    double num;

    scanf("%d",&t);
    for(i=1;i<=t;i++)
    {
        num = sqrt(x);

        if(ceil(num)==floor(num))
        {
            printf("YES");
        }
        else
            printf("NO");

    }
    return 0;
}
